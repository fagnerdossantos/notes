import 'package:flutter/material.dart';

// Paddings
const EdgeInsets defaultPadding = EdgeInsets.symmetric(
  horizontal: 8,
  vertical: 8,
);

// Space
SizedBox verticalSpace({double height = 20}) => SizedBox(height: height);
